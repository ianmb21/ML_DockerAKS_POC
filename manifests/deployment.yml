apiVersion: apps/v1
kind: Deployment
metadata:
  name: mydemo
spec:
  selector:
    matchLabels:
      app: mydemo
  template:
    metadata:
      labels:
        app: mydemo
    spec:
      containers:
      - name: mydemo
        image: mypocacr.azurecr.io/mydemo:v1
        resources:
          limits:
            memory: "3564Mi"
            cpu: "2040m"
        ports:
        - containerPort: 80
        imagePullPolicy: Always
